# Assets

This task uses only PsychoPy text, rectangle, and `ShapeStim` primitives. The
eight abstract outline coordinates are defined in `config/*.yaml`; no external
participant-facing media are required.

